# 统一换行：无论谁在什么系统上克隆，工作区里都是 LF。
# 否则 Windows 的 autocrlf 会把源码签出成 CRLF，测试里的多行正则会失配。
* text=auto eol=lf

# 二进制资源不参与换行规范化
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webp binary
*.icns binary
*.ico binary
*.dmg binary
*.exe binary
